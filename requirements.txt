requests>=2.32.4
beautifulsoup4>=4.12
PyYAML>=6.0

# Neobvezno: hitrejši in prizanesljivejši razčlenjevalnik HTML. Kadar ga ni
# (npr. ni paketa za tvoj Python), zajemalniki uporabijo html.parser iz Pythona.
lxml>=5.0; python_version < "3.14"

# Letake, ki so same slike (Lidl), sešijemo v PDF.
img2pdf>=0.5
Pillow>=10.0

# Branje in pisanje arhiviranih PDF za kopije samo z mesnimi stranmi.
# 6.16.1 popravi znane napake, pri katerih izdelan PDF zatakne branje besedila
# ali požre pomnilnik (PYSEC-2026-3655/3656/3910/3911/3912/3913).
pypdf>=6.16.1

# TLS prek sistemske shrambe certifikatov: deluje tudi za posrednikom podjetja,
# ki prestreza TLS s certifikatom lastne CA.
truststore>=0.10

# Gradnja .exe za Windows strežnike; za samo poganjanje ni potreben.
pyinstaller>=6.0; sys_platform == "win32"
